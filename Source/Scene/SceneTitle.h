#pragma once
#include "Scene.h"

//============================================================
// ■ タイトルシーンクラス
//   - ゲーム開始時のタイトル画面を管理
//   - Scene クラスを継承
//============================================================
class SceneTitle : public Scene
{
public:
    //--------------------------------------------------------
    // ■ 初期化
    //   - タイトル画面表示用のスプライト・テクスチャを初期化
    //   - 次シーンの初期値を設定
    //--------------------------------------------------------
    void Init();

    //--------------------------------------------------------
    // ■ 終了
    //   - 使用したリソース（テクスチャ・スプライトなど）を解放
    //--------------------------------------------------------
    void Term();

    //--------------------------------------------------------
    // ■ 更新
    //   - 入力処理（キー操作など）を行う
    //   - 次のシーンへの遷移判定
    //--------------------------------------------------------
    void Update();

    //--------------------------------------------------------
    // ■ 描画
    //   - タイトル画面の描画
    //--------------------------------------------------------
    void Render();

private:
    //--------------------------------------------------------
    // ■ タイトル画面用テクスチャ
    //--------------------------------------------------------
    Texture mTexture;

    //--------------------------------------------------------
    // ■ タイトル画面用スプライト
    //--------------------------------------------------------
    Sprite mSprite;
};
